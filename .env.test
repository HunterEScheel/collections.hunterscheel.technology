# Vitest loads this automatically. The unit tests never reach the network — these
# only exist so importing src/lib/supabase.ts doesn't throw on a missing key.
VITE_SUPABASE_URL=https://example.supabase.co
VITE_SUPABASE_ANON_KEY=test-anon-key
